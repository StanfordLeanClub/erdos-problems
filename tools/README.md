# Tools

Shared tooling should go here once it is mature enough to live in the repository.

Examples:
- frontier visualizer app code
- scripts that update status summaries
- helper scripts for generating or validating repo metadata

Keep problem-specific data (for example a problem's frontier YAML) inside the corresponding `problems/<number>/` folder.
